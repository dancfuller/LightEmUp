# Config with local IPs, API keys, room assignments
# Any location, not just backend/: a config.json written from the wrong cwd used
# to fall outside this rule and was stageable — and it holds local IPs + creds.
config.json

# Runtime clean-shutdown marker (power-recovery: planned reboot vs outage)
backend/.clean_shutdown

# Python
__pycache__/
*.pyc
*.pyo
backend/venv/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code local files (keep .claude/commands/ tracked)
.claude/settings*
.bash_out.tmp

# Throwaway test/investigation scripts
backend/test_*.py

# Rotating server logs (48h retention, written at runtime)
backend/logs/

# Preview harness deps + screenshots (tools/preview)
tools/preview/node_modules/
tools/preview/package-lock.json
tools/preview/*.png
